{
  "name": "bfts-ui",
  "type": "module",
  "scripts": {
    "build": "esbuild index.jsx --bundle --outfile=index.js --format=iife --loader:.jsx=jsx --loader:.css=css",
    "dev": "esbuild index.jsx --bundle --outfile=index.js --format=iife --loader:.jsx=jsx --loader:.css=css --servedir=. --watch=forever"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "esbuild": "^0.25.5"
  }
}
