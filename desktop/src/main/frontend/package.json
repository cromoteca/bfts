{
  "name": "bfts-ui",
  "type": "module",
  "scripts": {
    "build": "esbuild index.jsx --bundle --outdir=../../../target/classes/ui --format=iife --loader:.jsx=jsx --loader:.css=css",
    "dev": "mkdir -p dev-dist && cp index.html dev-dist/index.html && esbuild index.jsx --bundle --outdir=dev-dist --format=iife --loader:.jsx=jsx --loader:.css=css --servedir=dev-dist --watch=forever"
  },
  "dependencies": {
    "@vaadin/react-components": "^24.6.5",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "esbuild": "^0.25.5"
  }
}
