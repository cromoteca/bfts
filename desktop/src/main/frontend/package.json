{
  "name": "bfts-ui",
  "type": "module",
  "scripts": {
    "build": "esbuild index.jsx --bundle --outdir=../../../target/classes/ui --format=iife --loader:.jsx=jsx --loader:.css=css",
    "dev": "esbuild index.jsx --bundle --outdir=. --format=iife --loader:.jsx=jsx --loader:.css=css --servedir=. --watch=forever"
  },
  "dependencies": {
    "@vaadin/react-components": "^24.8.0",
    "@vaadin/vaadin-lumo-styles": "^24.8.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "esbuild": "^0.25.5"
  }
}
